# 更新记录

升级方法：在站点文件夹里打开命令行，运行 `npm run update`。数据在 Cloudflare 上，升级不会动它。

## 1.2.1 · 2026-09-25
- 图片在访客浏览器里缓存 30 天而不是 1 天，大幅减少 KV 读取次数
- KV 当天读取额度用完时返回可重试的错误而不是崩溃
- 教程常见问题：KV 新图延迟一分钟可见、每天 10 万次读取上限
- 数据库结构没有变化

## 1.2.0 · 2026-09-22
- 教程、压缩包、脚本和版本检查全部改走国内能连的 Cloudflare 镜像站（lune-share-docs.pages.dev），GitHub 只作备用
- npm 安装改用国内的 npmmirror 镜像源
- Node.js 下载提供阿里镜像说明
- 数据库结构没有变化

## 1.1.6 · 2026-09-22
- Cloudflare 邮箱未验证（8000077）时用中文说明怎么验证；教程第 1 步强调必须验证邮箱
- 数据库结构没有变化

## 1.1.5 · 2026-09-22
- 存密钥或部署失败时显示 Cloudflare 的具体报错并自动重试三次；项目名被占用时明确提示换名字
- 数据库结构没有变化

## 1.1.4 · 2026-09-22
- 登录报错的解决办法不再依赖 Chrome：先试 Safari 无痕窗口，再用国内能下载的 Edge
- 数据库结构没有变化

## 1.1.3 · 2026-09-22
- 登录 Cloudflare 时遇到 request_forbidden / CSRF 报错（多见于 Safari）会给出解决办法并允许重试，第二次只打印链接不再自动开浏览器
- 数据库结构没有变化

## 1.1.2 · 2026-09-21
- 装依赖不用再加 --legacy-peer-deps，直接 npm install 就行
- 数据库结构没有变化

## 1.1.1 · 2026-09-21
- `npm run update` 会顺手删掉模板已经不再包含的旧文件（比如 `schema.sql`）
- 数据库结构没有变化，升级不会跑迁移

## 1.1.0 · 2026-09-21
- 新增 `npm run update`：一条命令拉取最新代码、装依赖、跑数据库迁移、部署，自动保留你的配置和背景图
- 数据库改成带版本号的迁移（`migrations/`），以后加字段不会让老站报错
- 后台有新版本时会显示提示；「用量」页显示当前版本

## 1.0.0 · 2026-09-21
- 首个公开版本
- 从 1.0.0 升级：这个版本还没有 `npm run update`，第一次要先在站点文件夹里运行下面这条把升级脚本拿过来，之后就都是 `npm run update` 了：
  ```bash
  curl -fsSL https://lune-share-docs.pages.dev/scripts/update.mjs -o scripts/update.mjs && node scripts/update.mjs
  ```
